# Contributing

先に結論を書きます。**このプロジェクトは機能追加を受け付けていません。**

冷たく聞こえるかもしれませんが、断られる前提を知らずに時間を使ってもらうほうが
失礼だと考えています。

## このプロジェクトの位置づけ

recibir は SendGrid Event Webhook 受信のリファレンス実装です。
ライブラリではなく、汎用フレームワークでもありません。

「一つの妥当な設計を、動く形で読めるようにする」ことが目的なので、
設定項目を増やして適用範囲を広げることは、この目的に対してはマイナスに働きます。
分岐が増えるほど、設計上の判断が読み取りにくくなるからです。

したがって、以下は**方針として**お断りしています。良い提案でないからではなく、
このリポジトリの目的と噛み合わないためです。

- MySQL / Oracle / SQL Server など、PostgreSQL 以外への対応
- Spring Boot 3.x 系への後方互換
- 設定による挙動の切り替え（テーブル名、パッケージ名、永続化層の差し替え等）
- 新機能の追加（管理 UI、ダッシュボード、通知チャネルの拡充など）

必要であれば **fork してください。** Apache License 2.0 なので自由に改変・再配布
できます。それがこのコードの想定された使われ方です。

## 歓迎するもの

- **バグ報告。** 特に署名検証まわりの誤りは最優先で対応します
- **設計上の誤りの指摘。** SendGrid の挙動について事実誤認があれば直します
- **ドキュメントの誤り・不明瞭さ**

いずれも Issue でお願いします。**ただしセキュリティ上の問題は Issue ではなく
[SECURITY.md](SECURITY.md) の手順に従ってください。**

## Pull Request について

上記「歓迎するもの」に該当する修正であれば歓迎します。
それ以外の PR は、内容の良し悪しに関わらずクローズします。

事前に Issue で相談いただけると、お互いの時間が無駄になりません。

## メンテナンス方針

このリポジトリは**完成状態**を目指しています。
コミットが長期間止まっていた場合、それは放置ではなく、
現在の設計で意図した内容を表現しきっているという意味です。

以下の場合のみ更新します。

- セキュリティ上の問題が見つかったとき
- 依存ライブラリの重大な脆弱性
- SendGrid 側の仕様変更により、記述が事実として誤りになったとき

Spring Boot のマイナーバージョン追従は行いません。
